function handleProvCheck(req, res) {
    payload = req.body.number;

    // get the numbert from the payload
    // validate

    // send to fr

    // respond


    res.json({
        hey: "Your stuff"
    })
}

module.exports = {
    handleProvCheck
}