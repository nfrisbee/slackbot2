function handleProvCheck(req, res) {
    payload = req.body;

    // get the number from the payload

    // validate

    // send to fr

    // respond


    res.json({
        hey: "Your stuff"
    })
}

module.exports = {
    handleProvCheck
}